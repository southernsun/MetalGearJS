# Tasks — the endgame

## 1. Survey + implementation

- [x] 1.1 BossDefeatedLogic (the 0x3000 arm + SFX 0x53), DecNukeTimer cadence,
      DrawDestrucTimer (the CALL slot), ChkUseCigarettes (+2000, consumed),
      TextBoxExit's flag table (117/118/138)
- [x] 1.2 game.js: the countdown + the HUD digits + the cigarettes branch + the
      text-close flags; the ending screens documented out of scope (the escape banner
      stands in); BCD ammo documented display-equivalent

## 2. Checks

- [x] 2.1 endgame.headless.mjs: 7 checks — all suites green
- [ ] 2.2 User batch playtest (end of run)
