# Tasks — the lorry ride

## 1. Survey + implementation

- [x] 1.1 lorry.asm (the MovingLorries list, the 0x90 ride, the wobble table, text 91 +
      LorryMovTextF, the engine SFX 0x1F/stop 0x28)
- [x] 1.2 game.js: the 'lorry' game state + the shake translate + the engine loop; the
      room-104 ambush; export-actors comment-stripping fix (room 104's list was dropped)

## 2. Checks

- [x] 2.1 lorry.headless.mjs: 7 checks — all suites green
- [ ] 2.2 User batch playtest (end of run)
